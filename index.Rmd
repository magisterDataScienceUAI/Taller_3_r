---
title: "Bienvenidos"
knit: (function(input_file, encoding) {
  out_dir <- 'docs';
  rmarkdown::render(input_file,
 encoding=encoding,
 output_file=file.path(dirname(input_file), out_dir, 'index.html'))})
output:
  html_document:
    theme: united
    highlight: tango
---

En este sitio podrá revisar el taller número 3 correspondiente al curso  Lenguaje en R.

Para acceder de manera rápida al resultado le sugerimos pinchar el tabs correspondiente.